import initialState from './partners-initial-state';
import * as actions from './partners-actions';
import * as reducers from './partners-reducers';

export { initialState, actions, reducers };

export default { initialState, actions, reducers };
